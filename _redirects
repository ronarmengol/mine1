# Redirects are handled by netlify.toml, but we can keep non-conflicting ones here if needed.
# Currently netlify.toml handles www -> non-www and http -> https.
# We will leave this empty or add specific page redirects if needed later.
